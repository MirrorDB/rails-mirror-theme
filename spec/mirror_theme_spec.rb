require 'spec_helper'

describe MirrorTheme do
  it 'has a version number' do
    expect(MirrorTheme::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
